# Blitz
a fast key value store inspired by Redis

## implemented:
- hand rolled asynchronous event loop based on `epoll()`
- hand rolled binary serialization scheme and message protocol
- hand rolled hashtable with open addressing (robin hood hashing)

